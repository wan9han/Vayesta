__pycache__/
*.pyc

# SIESTA scratch files (only appear if siesta is run from the repo dir; runs
# normally live in dedicated work/output dirs, not here)
CLOCK
MESSAGES
INPUT_TMP.*
*.alloc
fdf.*.log

# benchmark 测试输出（gen_22680 ~37GB）
benchmark/gen_*/
benchmark/combine_*/
